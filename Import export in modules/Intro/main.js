function addition(x, y) {
  return x + y;
}

function subtract(x, y) {
  return x - y;
}

function Multiplication(x, y) {
  return x * y;
}

function divide(x, y) {
  return x / y;
}

export { addition as add, subtract as sub, Multiplication as mul, divide as di};
